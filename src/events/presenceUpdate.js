module.exports = async (oldMember, newMember) => {
	/*if (oldMember.user.bot) return;

	if (((oldMember || newMember).presence.game || (oldMember || newMember).presence) == null) return;

	if (oldMember.presence.game == null && newMember.presence.game != null) console.log('Started game');
	if (oldMember.presence.game != null && newMember.presence.game == null) console.log('Ended game');
	if (oldMember.presence.game.name != newMember.presence.game.name) console.log('Changed game');*/
};
